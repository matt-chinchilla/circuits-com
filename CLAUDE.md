# Circuit Center

Electronic components directory prototype — Vite React SPA + FastAPI + PostgreSQL + n8n, all in Docker.

## Prerequisites
Docker & Docker Compose · Python ≥3.12 · Node.js

## Commands

```bash
# Dev stack
docker compose up --build               # 5 services: db, api, frontend, n8n, nginx
docker compose down -v                  # stop + drop volumes

# API (from api/)
cd api && pip install -e ".[dev]"
pytest tests/ -v                        # ~1,750 tests, SQLite in-memory
alembic upgrade head
python -m app.db.seed                   # idempotent

# Frontend (from frontend/)
cd frontend && npm install
npm run dev                             # Vite on :3000
npm run build
npx tsc -b                              # type-gate (`tsc --noEmit`=NO-OP here: solution tsconfig, files:[]); `npm run build` too
npx eslint --ext .ts,.tsx src/          # boundary enforcement (exit 0=clean; --ext req'd, eslint 8)
npm test                                # vitest run (unit-logic only; *.test.ts, happy-dom per-file for DOM)

# Deploy (requires AWS CLI + ~/.ssh/id_ed25519 + commits pushed)
./deploy.sh                             # full deploy — INCLUDES nginx restart since 2026-06-02
./deploy.sh --frontend                  # frontend-only rebuild + nginx restart (faster path)
./deploy.sh --reseed                    # full deploy + clear/reseed DB (destructive)
./deploy.sh --status | --logs | --cert-renew

# Ops aliases (full reference: ALIASES.md at repo root)
circuits pull [--reporting|--catalog|--users]  # prod→local (catalog + users additive)
circuits push                           # local→prod catalog (additive; asks first)
circuits --fakeuser ...                 # presence fakes (see ALIASES.md)
```

Production: t3.small EC2 (`i-0d456bd12719e2176`), EIP `100.55.235.167`. Migrations + seed auto-run on api container start via `docker-compose.prod.yml`. Domains `circuitcenter.ai` (primary), `www.circuitcenter.ai` — all on one SAN cert at `/etc/letsencrypt/live/circuitcenter.ai/`.

Static analysis: TypeScript strict + ESLint at `frontend/.eslintrc.json` (boundary rules only — no Prettier). Frontend has a minimal vitest harness (`npm test`, unit-logic only — `*.test.ts` excluded from `tsc -b` + eslint via `tsconfig.app.json`/`.eslintrc.json`); API has pytest. Visual baselines at `tests/visual/baselines/`.

## Architecture

```
Browser → Nginx(:80/:443)
  ├── /        → Frontend(:3000)  Vite React SPA
  ├── /api/*   → API(:8000)        FastAPI
  └── /admin*  → Frontend(:3000)  React admin SPA
                    ↕
              PostgreSQL(:5432)
                    ↕
                n8n(:5678)         workflow automation (NOT in form path)
```

### API (`api/`)
FastAPI in `app/main.py`. Routers: categories/suppliers/search/manufacturers (public derived list)/forms/sponsors/parts/bom/auth/dashboard(+`admin_router`)/admin_messages/admin_sponsors/admin_expenses/admin_media/admin_presence/admin_manufacturers/admin_leads/admin_quotes/analytics/checkout/stripe_webhooks/feed_credentials/sitemap/calendar. Models: `Category` (self-ref tree, `parent_id`), `Supplier`, `CategorySupplier` (join association), `Sponsor` (XOR `category_id` OR `keyword`; single source for the banner), `Message`, `Part`/`PartListing`/`PriceBreak`, `Revenue`, `Expense`, `PageView`, `User`, `CalendarEvent`/`CalendarReminderSend`, `ActivityEvent`, `ProviderCredential`, `SupplierFeed`. Services: `category_service`, `search_service`, `auth_service`, `password_policy`, `rate_limit`, `aws_cost`, `activity`, `part_feed/` (registry+providers+importer), `email` (aiosmtplib → SMTP relay, demo mode when `SMTP_HOST` unset). SQLAdmin mounts at `/admin` on api but is unreachable in prod (nginx routes `/admin` → frontend). React admin = `frontend/src/admin/pages/`, JWT in `localStorage.admin_token` via `adminApi.ts`. Entrypoint: alembic → seed → uvicorn. Alembic head = **049** (039 part spec fields; 040 `page_views.country`; **041 part identity** — merges duplicate parts then adds `UNIQUE(manufacturer_id, upper(sku))` + `UNIQUE(part_id, supplier_id)`; 042 indexes `part_listings.supplier_id`; 043-046 leads/BOM/customer console; 047 `leads.distance_miles`; **048 `page_views` region/city/lat/lng + 049 `network`** — city+ASN GeoIP, see the geo bullet under Site analytics).

### Auth model (P1 overhaul, 2026-07-31 — migration 022)
`POST /api/auth/login` takes **`email` + `password`** — email is the ONLY login key (case-insensitive on `lower(email)`, unique via `uq_users_email_lower`); **no username fallback for any account**. `users` also gained `must_change_password` (BOOLEAN NOT NULL) + `password_changed_at` (TIMESTAMPTZ), and the `user_role` enum gained **`owner`**. Accounts: `matthew`, `Anthony`, `Daniel`, `Ronald`, `demo` — all `<name>@circuitcenter.ai`. **022's promotion (matthew→`owner`) + flagging (the 4 staff→`must_change_password`) are a one-time backfill of EXISTING rows** — `seed.py` still creates every admin as role `admin`, unflagged, so a FRESH local DB (migrations run before seed) has neither. Don't "fix" that divergence in seed without deciding what a brand-new environment should do.

- **Password policy** — EXACTLY 8-24 chars, ≥1 uppercase, ≥1 digit, ≥1 symbol. One backend home `app/services/password_policy.py` (`validate_password` → failed rule keys), mirrored 1:1 in `@admin/services/passwordPolicy.ts` (ASCII-anchored on BOTH sides so the JS `[A-Z]`/`\d` can't diverge; length counts code points). 422 body = `{code:'password_policy', message, unmet:[...]}` — the form ticks its checklist off `unmet` (`apiErrorDetail` only surfaces STRING details, so read `detail.unmet` explicitly).
- **Forced reset** — `get_current_user` 403s `password_change_required` for a flagged user; `get_authenticated_user` (no gate) backs the 2 exempt authed endpoints (`GET /auth/me`, `POST /auth/change-password`; `/auth/logout` takes no auth at all). Fail-CLOSED: every admin route already depends on `get_current_user`. Client: `adminApi` response interceptor raises `passwordGate` on that 403 (does NOT clear the token — it's still valid), `ProtectedRoute` redirects to `/admin/change-password`.
- **Session invalidation** — `POST /auth/change-password` stamps `password_changed_at`; `token_predates_password_change` 401s every token minted before it (no revocation table). The route returns a FRESH token so the caller isn't logged out by succeeding.
- **Rate limiting** — `app/services/rate_limit.py`, in-process, two namespaces (`login:*` / `recovery:*`, deliberately NOT shared) keyed per-IP AND per-account: 5 failures → 60s lock, doubling to a 15-min ceiling, cleared by success, decayed after 15 min. Counters are per-worker — the effective threshold multiplies by worker count (single uvicorn worker today).
- **Demo is exempt + credential-less** — `POST /api/auth/demo` (no body) mints a token for `settings.DEMO_LOGIN_EMAIL`, so no password ships in the JS bundle; it 404s (never 403) when disabled/missing. The demo email is REFUSED by `/auth/login` (`_is_demo_identifier`) — a public password that legitimately succeeds would also clear an attacker's own rate-limit lock. Demo is never flagged `must_change_password`. UI = the secondary "See Demo →" button on the sign-in screen.
- **Anti-enumeration is load-bearing** — unknown account → `verify_dummy_password()` (timing equalized) + the SAME counted failure + the same generic 401; recovery endpoints always return the generic OK. Never add a branch that reveals whether an address exists.

### Frontend (`frontend/`)
React 19 + TypeScript + Vite + SCSS Modules + Framer Motion. **Bounded contexts**:
- `src/public/` — public site
- `src/admin/` — admin SPA
- `src/shared/` — cross-scope ONLY

Aliases `@public/*` / `@admin/*` / `@shared/*` in `vite.config.ts` + `tsconfig.app.json`. **≥2-consumer rule** for `@shared/`. ESLint boundary: admin ↛ public, public ↛ admin, shared ↛ either.

**Routing** — public routes lazy-loaded except `HomePage` (eager for LCP). `PublicLayout` = thin `<Suspense><Outlet/></Suspense>` (NO AnimatePresence). Persistent `<BackdropLayer />` mounted ABOVE `<Routes>` in `App.tsx`. Heavy vendor split via `manualChunks`. **No charting library** — `Dashboard`/`Reports` inline native SVG; Recharts removed; do NOT reintroduce.

**Adding a public page**: `pages/<name>/{index.tsx, <Name>Page.module.scss}` + `lazy(() => import("@public/pages/<name>"))` + `<Route>` in App.tsx. Inner pages start with `<PageHeaderBand page="X" title="..." subtitle="..." />`. Anchor-scroll = namespaced ids + `scrollIntoView({behavior:"smooth",block:"start"})` + `scroll-margin-top:100px`.

**Adding an admin entity-CRUD**: `pages/<entity>/{list,form,detail}/index.tsx` + 3 lazy imports + 4 routes. Backend scaffold via `/add-model-field` (6 files).

**Keyword pages**: `/keyword` (landing) + `/keyword/:keyword` (sponsor profile). Both use `RequestModal`. Tier prices ($99/$299/$899) in `keyword-landing/constants.ts` are PLACEHOLDER. `useKeywordRequestModal` owns form state; hosting pages own open/close only.

**Category page**: column-header sort/filter (`ColumnHeader.tsx`, portaled popover) + sticky subcat pill-bar + client-side filter/sort/pagination (25/page). Parent pages fetch all parts (`per_page=500`); leaf pages same scoped to one sub. `?p=N` paginates. Subcats are NESTED pages — pill chips `<Link>` to `/category/:parent/:child` (no `activeSub`; column sub-filter stays).

**Catalog data**: 15 JSON files in `api/app/db/catalog_data/`. `_seed_real_catalog()` → Part/PartListing/PriceBreak. `_DEMO_CATALOG` for wizard demos. Revenue scaled by listing count via `_REVENUE_TIERS`. ~3,600 parts, ~41K listings, ~164K price breaks, 59 suppliers. Regenerate via `--reseed`.

**Per-qty best prices**: `_build_public_parts` + `_build_popular_parts` return `best_price` + `best_price_10/100/1000` via batched PriceBreak queries. `PartsTable` renders 4 price columns.

**Part page**: deep-links via `distributorUrl(supplier_website, part.sku)` (`DISTRIBUTOR_SEARCH` map, `endsWith` subdomain-tolerant, `{domain}/search?q=` fallback). Searches by MPN. Row `onClick` opens; guards `closest('a')` so inner `<a>` doesn't double-fire. Demo prices synthetic — real reliability needs price-feed integration.

**Part slugs**: `Part.slug` non-unique index (migration 008), `slugify_sku(sku.lower())`. `GET /api/parts/by-slug/{slug}`. Duplicate SKUs across manufacturers share the slug.

**Category SEO descriptions**: `Category.description` (migration 009). `CATEGORY_DESCRIPTIONS` in `seed.py`. Live ONLY in `<meta description>` + JSON-LD `CollectionPage` — not rendered visually.

**Site analytics**: `PageView` + `POST /api/track` (30/min per trusted IP). Inline SPA tracker in `index.html` patches `pushState`/`replaceState`, `sendBeacon`, skips `/admin`. `GET /api/dashboard/analytics` aggregates day/page/referrer/device/browser. Bot/human split is READ-TIME UA classification (`services/traffic_segments.py`, Humans is every panel's default segment — Meta's crawler alone was ~90% of raw views); country stamped at track time from the DB-IP mmdb. **GeoIP is THREE DB-IP Lite databases** (`api/app/data/`): country-lite COMMITTED (~8MB, the fallback), city-lite + asn-lite GITIGNORED (~124MB + ~9MB) and fetched in a cached api-image build layer — the month is RESOLVED AT BUILD TIME (walks today back two; `--build-arg DBIP_MONTH=` pins) because **DB-IP's CDN keeps only the current + previous month** — a hardcoded month 404s every backend build once it expires, and `docker builder prune -af` (the documented disk-reclaim step) busts the only cache masking it. `geoip.geo_for_ip` is fail-open (also try/excepted at the `/api/track` call site) and strips DB-IP's "City (District)" suffix — 16.6% of US lookups carry one, and `us_cities` groups by `(city, region)` with AVERAGED centroids so a metro stays ONE bubble. All geo columns are forward-only (ip_hash is one-way). The map panel: thermal inferno bins (`viewershipBins.ts` — luminance-monotone for CVD, measured), DOM legend below the canvas (never in-canvas visualMap — it painted over states when zoomed), wheel roam on BOTH views, click-a-state zoom (`usZoom.ts`), dot-click intel card (`cityIntel.ts`, keyboard path via the scrolling Towns list).

**Search v2**: one route `GET /api/search/` (richer contract in `@public/types/search`). Dropdown callers MUST pass `suggest=0` + `compact=1` (payload trim); results page omits both. Fuzzy did-you-mean = categories/suppliers/manufacturers only, NEVER part SKUs. `GET /api/manufacturers/` is DERIVED from parts GROUP BY — no CRM data behind it. Service caches (manufacturers/vocab/backfill, 600s TTL) are dropped by `invalidate_catalog_caches()` on catalog mutations.

**BrowseDrawer** (`@public/components/layout/BrowseDrawer/`) is the site's ONE drawer at every viewport (navMobileDrawer deleted): burger → rail+pane, z 105/110, inert+focus-trap.

**SEO**: `robots.txt`, dynamic `sitemap.xml` via `GET /api/sitemap.xml` (URL count tracks the catalog; part URLs are SLUGS, not ids). Head tags come from TWO paths and both matter: a **build-time prerender** writes the served HTML (`frontend/scripts/seoPrerender.ts`, a Vite plugin → one doc per manifest route; ~12k local as of 2026-08-21), and `react-helmet-async` (`HelmetProvider` in `main.tsx`) re-renders the same tags client-side after hydration via `@public/components/PageHead`. Both read ONE source, `@public/services/seo{,Routes}.ts` — change tags there, never in a page. Home: `WebSite`+`SearchAction`+`Organization`. Category: `CollectionPage`+`BreadcrumbList`. Part: `Product` (**no `offers`** — demo prices are synthetic, and offer markup that disagrees with the real distributor price is a manual action). HSTS in `nginx.ssl.conf`. Gzip + `Cache-Control: immutable` on hashed assets. Search page `noindex, follow`.

**Route prefetching**: `App.tsx` uses `requestIdleCallback` to eager-import top 5 chunks. All `import()` calls `.catch(() => {})` to suppress post-deploy noise. **Hover prefetch** on `CategoryCard.tsx` + `SearchBar.tsx` via `onMouseEnter`.

**Service Worker**: `vite-plugin-pwa` runtime-cache only (no precaching). `StaleWhileRevalidate` on `/api/categories/*` (60s); `NetworkFirst` on other public API. `cleanupOutdatedCaches: true`, `manifest: false`, `navigateFallback: null`.

**Ultrawide (>1600px)**: `.subnavInner`, `.headerInner`, `.contentWide` all widen to `90vw`. `.contentWide max-width: calc(90vw + 56px)` so `.contentInner` aligns with `.chipBar`.

### Admin layout (`AdminLayout.tsx`)
240px Phosphor-Light sidebar + 64px sticky topbar (Lucide). Parts/Suppliers/Import badges: demo → `DEMO_BADGES`, live → `adminApi.getStats()`. Bell + Messages badges sync on `[location.pathname]`. Admin chrome intentionally un-themed.

### Claude Code Automations (`.claude/`)
- **Hooks** — PreToolUse: blocks `.env`/lock edits, warns on `api/app/admin.py`, `migration-safety-check.sh` on commits. PostToolUse: tsc on .ts/.tsx/.scss; pytest on .py; ruff format+check (excl alembic); `scss-lint.sh`; `frontend-rebuild.sh` (flock).
- **Ruff** (`api/pyproject.toml`): line 100, py312, E/F/W/I/UP/B, ignores E501+B008, excludes `alembic/versions/`.
- **Agents** — `deploy-preflight`, `seo-auditor`, `visual-regression-guard`, `frontend-perf-auditor`, `theme-persistency-guard`. `seo-auditor` keeps a findings ledger at `.claude/seo-audit/findings.json` (committed, not ignored) so a re-run reports FIXED/REGRESSED/STILL OPEN instead of starting blind; it re-verifies each prior finding rather than trusting a commit message, and fixed findings STAY in the file so a regression is detectable.
- **Skills** — `seo-writer`, `/add-model-field`. **MCP** — `context7`, `chrome-devtools-mcp`, `playwright`.
- **HeroColorTuner** — dev-only IC-opacity tuner, gated at App.tsx.

### Data flow
- Forms POST → API → `BackgroundTasks` → `email.send_*_notification` (aiosmtplib → the `SMTP_*` relay; AWS SES in prod). n8n still in compose, NOT in form path.
- Parts attach to subcategory in `seed.py` (conftest too). Aggregates roll up `own + sum(children)`.
- Parent "Popular Parts" rollup: `category_service._build_popular_parts(db, parent_id, page, per_page)` — `WHERE category_id IN (self + children)`, GROUP BY part, ORDER BY SUM(stock) DESC. `<Pagination>` at `@public/components/widgets/Pagination.tsx`. `POPULAR_PER_PAGE=12`.

## Key Patterns

### SCSS Modules
```scss
@use '../../styles/variables' as *;
@use '../../styles/mixins' as *;
@use '../../styles/animations';
```
Mixins in `_mixins.scss`: `container`, `card-base`, `hover-lift`, `responsive($bp)`, `gold-shimmer-border`, `skeleton-shimmer`, `scrollbar-thin($height,$radius)`, `truncate` (single-line ellipsis, `min-width:0` for flex), `line-clamp($lines:3)`, `tap-target($inset:-9px)` (::after hit-area expansion for <44px controls). `$nav-height-mobile: 48px` in `_variables.scss`. Hoist repeated literals here before they sprout four places.

### Framer Motion page transitions
```tsx
initial={{ opacity: 0, x: 20 }}
animate={{ opacity: 1, x: 0 }}
exit={{ opacity: 0, x: -20 }}
transition={{ duration: 0.15, ease: 'easeInOut' as const }}
```
PublicLayout = `<Suspense><Outlet/></Suspense>` — `AnimatePresence` REMOVED here (left entering pages stuck at prior exit-state). Local UI like `keyword/index.tsx` chip-stagger keeps it. **Navbar MUST be sibling of `<Routes>`** in App.tsx — not inside a transforming ancestor (sub-pixel text blur).

### Theme system
4 themes (`base`, `steel`, `schematic`, `pcb`) in `_themes.scss` as CSS custom properties on `[data-theme]`. **Steel is prod default** — `ThemeBridge.tsx` hardcodes `theme="steel"` in prod (URL + localStorage ignored). Dev: URL `?nav=A|B|C` → `localStorage.circuits.nav.theme` → `DEFAULT_THEME`. `_themes.scss` requires `@use 'variables' as *;` for `#{$font-heading}` interpolation.

**Hero SVG cascade** — `CircuitTraces.module.scss` defines 6 CSS custom properties at `.circuitTraces` ROOT (NOT per child — 140 paths × var() re-resolutions kill perf). Apply SVG filter at a `<g>` wrapper, NEVER per-path. Base theme carve-out suppresses shared glow via `.traceGroup { filter: none; }`. `<CircuitTraces variant="full" />` pinned everywhere. `viewBox 0 0 1200 400` w/ `preserveAspectRatio="xMidYMid slice"` — screen→viewBox y-map SKEWED; use `getScreenCTM()` or source coords.

**Edge-connector pattern in `CircuitTraces`** (CN1 left, CN2 right): pass-through connectors w/ dual-side pin rows, board traces clipped to inner pins, short outer stubs continue off-board. Through-electrons fade via `<animate attributeName="fill-opacity" values="1;1;0;0;1;1" keyTimes={hideKeyTimes}>` synced (same `dur`/`begin`/`repeatCount`) to sibling `<animateMotion>`. keyTimes are arc-length fractions (paced calcMode); inner `0;0` must span FULL body. Constants `CN1_PINS`/`CN2_PINS`/`CN2_OUTER`/`CN1_HIDE` module-level above `ELECTRONS`.

HeroSection + PageHeaderBand are TRANSPARENT layout-only — band is a "window" onto persistent backdrop. Pause wiring: `IntersectionObserver` + `visibilitychange` (`svg.pauseAnimations()` + `data-paused="true"`). `animationend` releases completed animations.

### API conventions
All routes prefixed `/api/` (router prefix). **`routes/forms.py` is `/api`, NOT `/api/forms`** — endpoints `POST /api/contact`, `/api/join`, `/api/keyword-request`. Frontend uses relative paths. `part_to_dict()` returns `category_name`/`slug` + `parent_category_name`/`slug` (`| null` top-level). Category API exposes siblings via `ParentCategoryResponse.children` (`lazy="selectin"`). `SubcategoryChips` URL-driven, `aria-current="page"` on active.

### Contact page motif
Datasheet card — founders labeled U1/U2 (schematic designators), crop-mark corners, PCB grid bg (24px cells, $nav-blue @ 3.5%). Don't flatten. Emails/phones are plain `<a href="mailto:|tel:">` (no JS handler, PCB-grid/crop-mark overlays are `pointer-events: none`) — an "email app won't open" report is ENVIRONMENTAL (no OS default mail handler / corp browser protocol policy), NOT a code bug; don't add an onClick to "fix" it.

### Navbar — pinned-edge layout
Brand (`left: 20px`) + nav+LOGIN (`right: 20px`) are `position: absolute` on `.topStrip`. Search bar centered via `left: 50%; transform: translateX(-50%)`, hidden on `/`. Do NOT use Grid or `space-between` (breaks on narrow side-track content).

### Footer + ErrorBoundary + overflow guard (site-wide)
- `<Footer />` mounted ONCE in `PublicLayout.tsx` (sibling of `<Outlet />`); `min-height: calc(100vh - $nav-height)`; footer `margin-top: auto`. Theme-aware.
- `<ErrorBoundary key={location.pathname}>` wraps Outlet (public) + Routes (admin) at `@shared/components/ErrorBoundary.tsx`. Pathname-keyed → auto-clears on nav.
- `html, body { overflow-x: clip }` in `global.scss` (NOT `hidden` — disturbs `position: sticky`). Companion: `.contentWide { min-width: 0; width: 100% }` breaks flex min-content chain.

### SPA scroll-to-top
`App.tsx`: `useEffect(() => { if (location.hash) return; window.scrollTo({top:0,left:0}); }, [location.pathname])`. Hash skip preserves anchor nav.

### Seed data (idempotent)
15 cats, 75 subcats (5 × 2 levels), 7+ suppliers, 2 sponsors, 59 parts/179 listings/193 revenue (plus real-catalog). `get_or_create_*` skips existing. `CATEGORY_DATA` slugs EXPLICIT per row — `get_or_create_category(name, slug, ...)` requires slug arg (some like `motor-motion-ics` don't match `slugify(name)`).

**Seed data carries NO phone numbers (owner rule, 2026-08-15)** — a rendered number must come from the company itself, and checkout collects none. The old seeds were all someone else's digits: invented ones on fictional sponsors, staff personal cells on the `* Test` companies, real support lines on distributors shown as "partners". Both DBs were blanket-nulled; boards guard absence (`{phone && …}`, Platinum rail dashes) and the pitch preview never links contacts. Test fixtures use reserved `555-01xx` only. **Carve-out (owner, 2026-08-20): the INTERNAL lead roster** (`seed_data/leads.csv` → the admin-only, demo-refused Leads CRM) seeds real collected outreach contacts including phones; the rule still bans phones on all PUBLIC surfaces.

## Gotchas

(Each is a recurring trap. One-liners by design. A bullet ending in `→ docs/claude-gotchas/<topic>.md` is the COMPRESSED rule — read the pointed file for the full history/evidence BEFORE working in that area, and update BOTH when a rule changes.)

- **TS strict** — remove unused vars, don't prefix `_`.
- **`?:` catches only `undefined`, NOT `null`** — Python `None` → JSON `null` slips through `field?: number`. Use `field?: T | null` + `!= null`; default w/ `??`.
- **`/admin` in prod = React SPA**, not SQLAdmin. Edit `frontend/src/admin/pages/`, not `app/admin.py`.
- **Adding a Supplier field = 6 files**: model + alembic + Response/Create/Update + `to_dict` + TS type + List COLUMNS + FormPage. Use `/add-model-field` skill.
- **`SupplierResponse`/`CategoryResponse` are shared by public AND admin** — fields appear in unauthenticated routes. Use separate auth-gated endpoints for admin-only fields.
- **`response_model=SchemaX` silently strips computed attrs** — `obj.parts_count = N` stamps work locally; Pydantic's `from_attributes=True` drops them. Drop `response_model=` or add the field w/ default.
- **`routes/sponsors.py` + `routes/admin_sponsors.py` are symmetric `SponsorResponse` consumers** — any new field touches BOTH sites or Pydantic defaults silently to `None`.
- **Sponsor placement = write-side BLOCK + read-side filter (2026-06-22; was supersede)** — single-slot placements (Platinum on a top-level cat, Gold on a child) hold ONE active sponsor. `admin_sponsors._reject_if_slot_taken(...)` runs on POST+PATCH and rejects a 2nd active same-tier sponsor with **409** — the incumbent (oldest active) keeps the slot (was: `_supersede_existing_for_category` Expired the old one so the NEWEST won). `category_service` reads the single active sponsor (tier-filtered, Active OR NULL). To re-sell a slot the admin expires/removes the current sponsor first. **Exempt: Silver** (subcategory directory, multi) AND **keyword** (multi per keyword) — never blocked.
- **Sponsorships are the SINGLE source of truth (migration 011)** — the banner reads ACTIVE `sponsors` rows, tier-ordered, via `get_category_partners` (`/partners`), NOT a join flag. `CategorySupplier.is_featured`/`rank` DROPPED (pure association, cascade only). /admin/sponsors ↔ banner 1:1 — deleting a sponsor removes the company, no side-effect.
- **Sponsor tier↔placement matrix** — top-level = Platinum only; subcategory = Gold (single-slot) + Silver (multi); keyword = Silver/Gold. Enforced in THREE agreeing places (PG trigger `sponsor_tier_placement_check`, `_validate_tier_placement`→422, admin `choosePlacement`); single-slot BLOCKS w/ 409 (incumbent = oldest active keeps the slot) and the PATCH guard must ALSO fire on `status`→active; migration 016's partial unique indexes are the DB backstop. → docs/claude-gotchas/sponsor-boards.md
- **`Sponsor.status` filters MUST treat NULL as Active** — legacy seed omits status. `status != 'Expired'` is UNKNOWN for NULL → silently skipped. Use `or_(status=='Active', status.is_(None))`.
- **Admin sponsor form = 3-way segmented placement** (`top-category|subcategory|keyword`) — `placementDerivedRef` MUST reset on `[id]` change (separate `useEffect`) or cross-edit nav keeps the prior bucket; all 3 buttons → `choosePlacement(p)`. Gating (2026-06-02): Category=`disabled={tier!=='Featured'}`, Subcategory+Keyword=`disabled={tier==='Featured'}`. Top-level + subcategory dropdowns are a custom `IconSelect` listbox (native `<select>` strips `<Icon>` markup) w/ `aria-activedescendant` a11y. Tier `<select>` `data-tier` → tier badges (`TIER_OPTION_STYLE`).
- **Sponsor XOR is Postgres-only** (SQLite skips CHECK) — enforce client-side in SponsorFormPage `validate()`/`buildSponsor()` AND server-side in router (422). Never both fields set or both empty.
- **Sponsor `tier` casing — normalize at EVERY read site** — admin emits TitleCase, legacy seed/DB store lowercase (`'platinum'`/`'gold'`); `tier` is a free string (no enum). Client-side: ALWAYS `@admin/services/sponsorTier.normalizeSponsorTier` (the single home + `SPONSOR_TIER_RANK` / `isActiveSponsor`(Active|null) / `countActiveSponsorsByTier`). A TitleCase-only comparison silently DROPS lowercase rows — 2026-06-14: lowercase `'platinum'` showed **None** on Suppliers, was uncounted/unfilterable on Sponsors-list, and drove a unrefreshed Featured slice on the Dashboard chart. Seed `get_or_create_sponsor` `.capitalize()`s on write; prod normalized once via `UPDATE sponsors SET tier=initcap(tier)`. Server-side: `_is_featured(tier)` (`.strip().lower()`), never `tier == 'Featured'` literal. Lowercase in JSX for `[data-tier='X']`.
- **Tests use SQLite via `Base.metadata.create_all`** — SQLite ignores `String(N)` length AND CHECK. For length contracts assert on metadata: `Model.__table__.c.col.type.length >= N`. **`postgresql.UUID` renders as bare `UUID` on SQLite → NUMERIC affinity → ~1e-6 of uuid4 hex coerce to float (`'float' has no .replace`) = intermittent ~34%/run seed crash.** Fix = conftest `@compiles(UUID,'sqlite')`→`CHAR(32)` (TEXT affinity); guard `test_uuid_sqlite_affinity.py`. The per-test `engine.dispose()` only gives data isolation — it never fixed the float.
- **Parts→category attachment** — `conftest.py` to subcategory; `seed.py` to top-level. Aggregates roll up `own + sum(children)`.
- **`Part.sub_slug` auto-derive** — `create_part` stamps `sub_slug = child.slug` when `category_id` is a child. Migration 006 backfills (PG-only).
- **`featured_supplier_name` batch-query** — `get_all_categories` JOINs `sponsors` (active) per category, tier-ordered, dict-comp → `featured_suppliers[0].name`. No N+1.
- **`POST /api/parts/` atomic create** — `body.initial_listing` creates Part + PartListing in one txn.
- **Deleting a Supplier cascades 8 surfaces** — order: PriceBreak → PartListing → Sponsor (NOT NULL FK) → CategorySupplier → Revenue, NULL `User.supplier_id`, NULL `ActivityEvent.supplier_id` (history is kept, unlinked), DELETE the `supplier_feeds` row (dependent, not history), then delete. Mirrors `delete_part`.
- **Bulk `query(...).delete()` + `lazy="selectin"` → "Dependency rule tried to blank-out primary key"** on parent delete. Fix: `db.expire(parent)` between bulk-deletes and `db.delete(parent)`.
- **Pyright "Import app.services.email could not be resolved"** is a false positive (stdlib `email` shadows). Runtime fine.
- **`dict(query(Col1,Col2).all())` mis-types under Pyright** — use `{row[0]: row[1] for row in query.all()}`.
- **Framer Motion v12 requires `as const` on ease**.
- **Framer Motion `whileHover` FIRES ON TOUCH** via Pointer Events hover synthesis — card "jumps" on tap. Use CSS `@media (hover: hover)` for desktop-only.
- **Don't reintroduce `AnimatePresence` around `<Suspense><Outlet/></Suspense>`** — FM12 leaves second-nav entering motion.div stuck at prior exit-state.
- **Don't gate visible content on JS-added classes inside `AnimatePresence`** — IO callbacks fire unreliably mid-transform, leaving `opacity: 0` stuck. Default visible; trigger via `setTimeout`.
- **CSS `opacity: X` OVERRIDES SMIL `<animate attributeName="opacity">`** — CSS wins over SVG presentation-attribute animation. Animate `fill-opacity`/`stroke-opacity` instead.
- **SVG `<filter>` on `<g>` whose children animate = CPU raster on mobile** — `feGaussianBlur` is the lag cause. Apply at `<g>` wrapper, never per-path. Mobile: `.traceGroup { filter: none }` ≤768px.
- **Never animate CSS `drop-shadow()`** — scroll lag. Use static shadows.
- **`filter: hue-rotate(0deg)` is NOT free** — promotes to compositor layer even at 0deg. Gate behind non-default themes.
- **Sub-pixel text blur from `transform: translate*(-50%)`** — fractional px + GPU layer = subpixel glyph raster. Use `top:0; bottom:0; display:flex; align-items:center`.
- **`Node.contains(window)` THROWS** — scroll-close/outside-click guards must `e.target instanceof Node && ref.current?.contains(e.target)`.
- **`setPointerCapture` retargets `pointerup` + synthesized `click`** — child `<a>`/`<button>` taps silently stop. Guard: `if (e.target?.closest('a,button,[role="button"],input,textarea,select,label')) return;` before capture.
- **iOS Safari fires `pointercancel` (NOT `pointerleave`) under `setPointerCapture`** when a system gesture preempts. Listen for both plus `pointerup`.
- **`touch-action: pan-y` on touch-interactive surface CANCELS `pointermove`** once browser commits vertical scroll. Use `touch-action: none` for full tracking (trade-off: not a scroll surface).
- **SponsorBlock = the Gold subcategory flashlight PCB card** (`SponsorBlock.tsx`) — `touch-action:none` + `setPointerCapture` (skip interactive descendants), NO `whileHover`; flashlight is COMPOSITOR-ONLY (fixed transform-window + static mask + counter-translated inner; `will-change` only under `[data-lit]`); `preserveAspectRatio="xMidYMid meet"`. → docs/claude-gotchas/sponsor-boards.md
- **Platinum `<canvas>` tile field (`csFx.tsx`) — the "alive" PCB** — the perpetual ~60fps loop while on-screen IS the intent; do NOT idle-pause (user-rejected 2026-06-21); only culling is its IntersectionObserver + `visibilitychange`. NEVER alter the visual consts (GAP 19 / shimmer .36 / dome R 72 / flip .34). Badges: Platinum "Exclusive Partner", Gold "◆ PREMIERE PARTNER". → docs/claude-gotchas/sponsor-boards.md
- **csFx canvas loop must NOT resurrect after unmount (leak fix dfd465d)** — a `destroyed` flag makes `start()`/`wave()` no-ops post-`destroy()`; `wave()` clears its timeout ids. Rate interaction-then-nav leaks by the INTERACTION path (wrap `requestAnimationFrame`, count calls/sec) — nav-only probes under-rate them. → docs/claude-gotchas/sponsor-boards.md
- **"Animations frozen right after a deploy" ≠ a code regression — suspect a STALE CACHED BUNDLE first** — hard refresh fixes it; diagnose before reverting: several independent effects dead from a single-file change ⇒ shared/client cause; MEASURE the live site (FULL-canvas pixel-sample over ~24 rAF frames — a corner sample false-reports "static"); check `prefers-reduced-motion`. → docs/claude-gotchas/frontend.md
- **Sponsor boards, one source `sponsors`** — Platinum `CategorySponsor.tsx` via `/partners` (`csFx.snapshotCircuit` MUST strip `stroke-dash*` off the SVG clone); Gold `SponsorBlock` (`category.sponsor`); Silver `SilverPartners.tsx` (always U1–U5, empty → `/contact`). `CategoryPartnersBanner` ALWAYS renders `<CategorySponsor>` (Open-Placement when null). Mobile: `.tierRowMain` MUST `flex:0 0 auto` ≤900. → docs/claude-gotchas/sponsor-boards.md
- **Open-placement (UNSOLD) sponsor CTAs are tier-partner-named** — Platinum "Become An Exclusive Partner", Gold "Become a Premier Partner", Silver "Become a Preferred Partner" (arrow glyphs as entities). The PITCH-PREVIEW takeover must NEVER render live contact links — inert placeholders only (the slot belongs to no company yet). → docs/claude-gotchas/sponsor-boards.md
- **Silver board "◆ PREFERRED PARTNERS" floating pill (2026-06-22)** — `.svp-badge` mirrors Platinum's floating `.csb-badge` (absolute, `top:11px`, `left:50%`+`translateX(-50%)`, `aria-hidden`, `&#9670;` diamond entity). `.svp-headrow` `padding-top:52px` clears it on desktop; mobile (`≤768px`, where `.svp-headrow` is `display:none`) the chip stack needs `.svp-stack{padding-top:46px}` or the pill overlaps the first chip. Silver stays equal-height to Gold (external `.tierRowMain` absolute sizing — internal padding doesn't change board height; verified 343px==343px).
- **Subcats NESTED `/category/:parent/:child`; flat `/category/:slug` redirects** — both routes → one `CategoryPage`. Canonical redirect MUST be an EFFECT (`navigate(...,{replace})`), NOT render `<Navigate>` — `ErrorBoundary key={pathname}` drops the rendered child's nav-effect on remount (empty page, frozen URL). `?p`-reset effect bails while redirecting (`if (needsCanonicalRedirect) return`). URLs via `categoryPath(slug, parentSlug?)`; sitemap nested.
- **`<CircuitTraces variant="full" />` is hero-ONLY** — once, in `BackdropLayer` (App.tsx, above `<Routes>`, persistent across nav). ALL other instances (incl. Silver `SilverPartners`) use `variant="static"`. `full`=14 SMIL loops+6s draw+IO bookkeeping; a 2nd `full` on a non-hero route breaks the perf invariant. (Silver was `full` until 2026-06-21 — reverted to `static`: its IO-gate only pauses when OFF-screen, but users hover it ON-screen, so the 14 loops starved the hover raster budget and the hover effect trailed the cursor.)
- **Fixed-angle 3D *decoration* = ONE vector `<svg>`, NOT CSS-3D `preserve-3d`** — preserve-3d is O(faces) GPU layers → iPhone DPR-3 OOM crash + flicker. Login `IsoBoard`: desktop CSS-3D / mobile SVG (BOTH project `isoGeometry.ts`); `transform: scale()` cuts ZERO GPU cost; NEVER `will-change`/`overflow:clip` a preserve-3d node on mobile; SVG @keyframes live in the sibling PLAIN `.scss`. → docs/claude-gotchas/frontend.md
- **Verifying animations via chrome-devtools: sample ACROSS real frames** — a busy-wait + `getComputedStyle` returns a FROZEN snapshot (the anim clock doesn't advance on a blocked main thread → false "not moving"). Confirm motion with async rAF sampling (`requestAnimationFrame`+`setTimeout` over a few frames → `offset-distance`/`transform` advancing). Real iOS-device GPU bugs (DPR-3 OOM crash) are NOT reproducible in chrome-devtools at all → iterate via the user's device; trust the served CSS.
- **`mix-blend-mode: screen` + per-pointermove CSS-var updates = full-banner CPU recomposite** — the moving gradient center defeats the compositor's blend cache; `mask-image` on a sibling compounds it. Use a pre-baked rgba radial-gradient (`will-change: background`) + stacked `background:` linear-gradient fades instead. Deleted twice (v11.2 cursor-lamp, v15 `useFlashlight`) — do not reintroduce.
- **Category page = opacity-only fades + layout invariants** — no entrance animation here (re-fires on subcat remount); shell renders SYNC from `categoryShellMemo`; `.chips` no margin-top; busy skeletons reserve REAL heights; NEVER `min-height` on `.chipBar`; chips are `<button>`. Parts+counts cached in `categoryDetailMemo` (LRU 12), read SYNC in `useState` initializers; part mutations MUST wrap `bustingAfter` or counts/SKUs go stale. → docs/claude-gotchas/frontend.md
- **`.outletWrap { position: relative; z-index: 1 }` is load-bearing** — without it BackdropLayer (z:0) paints ON TOP of static page descendants.
- **Inner-page surface-bg goes on a body WRAPPER inside motion.div, NOT on motion.div** — `<BackdropLayer />` (z:0, top: $nav-height, height: 420px) needs visibility through the band. PageHeaderBand + HeroSection are TRANSPARENT.
- **URL-param-absent ≠ default-intent** — default-button click clearing a URL param is shadowed by stale localStorage. Write default to localStorage SYNC before `setParams`.
- **Don't put `setSearchParams` in `useEffect` deps** (RR v7) — identity changes on URL change; effect that "resets `?p` on filter change" also fires on page change. Depend only on filter values; functional form `setSearchParams(prev => ...)` needs no setter dep.
- **State-dep effect + async fetch needs cancel-flag** — `let cancelled=false; ...; return () => { cancelled=true; }`; gate `.then`/`.catch` on `if (cancelled) return;`. AdminLayout badge fetch is canonical.
- **Dev-only components gate at the CALL SITE** — `{import.meta.env.DEV && <X />}` at App.tsx. Don't `if (!DEV) return null` before hooks (Rules of Hooks).
- **Non-ASCII glyphs in JSX text get mangled to `\uXXXX` literals** by edit tooling — `>↗<` ends up rendering the literal 6-char string. Use HTML entity (`&#8599;`) or JS expression (`{'↗'}`).
- **Empty SCSS rule → undefined CSS module class** — `.foo {}` makes `styles.foo === undefined`. Always include ≥1 declaration.
- **CSS Modules can't host BEM `--`** — `qa-card--primary` becomes invalid. Use camelCase (`qaCardPrimary`); compose at call site.
- **Source-line grep undercounts JSX rendered DOM** — `.map()` multiplies (CircuitTraces: 54 src `<rect>` → 193 rendered).
- **Structural-rename: grep `import.*\.scss` in TS/TSX**, not just `@use` in SCSS — side-effect imports (`import './styles/global.scss'`) bypass `@use`.
- **`border-collapse: separate` for rounded table corners** — `collapse` ignores cell `border-radius`. Use `separate` + `border-spacing: 0` + corner-cell radii. Borders on `<tr>` no longer render — move `border-bottom` to `.td`.
- **Buttons inherit `line-height: 1.6`** from body — overflows height-constrained rows. Fix: explicit `line-height: 1` + padding.
- **`<input type="url|email|tel">` silently kills form submit** for HTML5-invalid values — React `onSubmit` never runs, no `:invalid` styling, no console error. Use `type="text"` + `inputMode` + JS validation + `noValidate`. Guard: `api/tests/test_no_type_url_form_input.py`.
- **`prependScheme` must be RFC-3986-aware** — naive `!startsWith('http')` produces `https:////acme.com` or `https://mailto:…`. Skip already-schemed via `^[a-z][a-z0-9+.-]*:` OR `^//`.
- **Sponsor/external website hrefs MUST go through `safeHttpUrl` (`@shared/utils/url`), NOT raw `prependScheme`** — prependScheme PRESERVES an existing scheme, so a DB/admin `website` of `javascript:`/`data:`/`vbscript:` flows straight into `href` = stored DOM-XSS. `safeHttpUrl` prepends a scheme THEN validates http(s) via `new URL` (resolves `//host`→https), returns `null` to hide the link. All 3 sponsor links use it (Platinum coname, Gold/Silver "Visit Website") + `rel="sponsored noopener noreferrer"` (paid links). Guard `frontend/src/shared/utils/url.test.ts`.
- **Sponsor/supplier LOGO `<img src>` MUST go through `safeImageUrl` (`@shared/utils/url`), NOT `safeHttpUrl`** — logos may BE data-URLs; `safeImageUrl` allows http(s)+raster `data:image`, rejects svg/html/js. FOUR render sites: `CsLogo`, `SbLogo`, `svp-markimg`, and the keyword profile hero (the easy-to-miss 4th). Write-side: `validate_optional_image_url` Pydantic validator. → docs/claude-gotchas/sponsor-boards.md
- **Admin image upload = base64 data-URL in DB, NO upload endpoint (2026-06-22)** — `Sponsor.image_url` + `Supplier.logo_url` widened `String(500)→Text` (migration 017). Shared `ImageUploadField` (`@admin/components`, drives BOTH sponsor + supplier forms) does file→**`LogoCropperModal`**→`canvasToDataUrl` (`@shared/utils/image`: 256×256 SQUARE cover-crop, WebP 0.82→JPEG 0.85, ≤64KB; `fileToDataUrl` DELETED 2026-07-10, `loadImage(url, crossOrigin?)` exported) OR paste-URL (never cropped — cross-origin) — both write the same `value` (typed `string | null`). Chosen because the **API container has NO volume mount** (disk uploads wiped each rebuild) but the DB has a volume; public `<img>` renders data-URLs natively. `Supplier.logo_url` (pre-existing col, now form-settable + admin list/detail thumbnail) feeds ALL tier boards — Gold/Silver fall back `image_url ?? logo_url` (Platinum already did).
- **Logo crop + sponsor brand colors (migration 018)** — `LogoCropperModal`'s geometry authority is the MEASURED `frameSize`, never the 320 const; `brand_takeover` stamped at BOTH construction sites (`_sponsor_board_dict` AND the keyword route); sponsor form requires BOTH colors or NEITHER; `safeHexColor` gates EVERY stored-hex→inline-style path (+ `validate_optional_hex_color` server-side). → docs/claude-gotchas/sponsor-boards.md
- **Circle completion at Gold/Silver/keyword is gated on `isDataImage(src) && naturalWidth===naturalHeight` in `onLoad`** — NOT data-URL-ness alone (legacy rectangular wordmarks keep letterbox); cropped classes size FLUSH to the rim (`calc(100% - Npx)` + `object-fit: cover`). "my logo's corners got cut" reports trace here. → docs/claude-gotchas/sponsor-boards.md
- **csFx dot-grid follows SECONDARY, underglow is PINNED (2026-07-11, a995e18)** — `refreshColor()` computes `col.dot` from `col.acc` (the `--gold` probe) ONLY AFTER that probe resolves (the assignment was RELOCATED, not recolored in place — the old site would read the prior refresh's stale accent), darkened `×0.3+8` so an unbranded board's resting grid doesn't brighten ~5×. The lifted-tile underglow sprite (`buildGlow`) reads a NEW `col.glow` sourced from a NEW CSS var `--underglow` (base `.csb` `#e8c252`, Platinum `.csb.csbA` `#c5bfd6`) that `brandVars()` NEVER sets — the glow is the SAME stock color for every sponsorship, branded or steel, by BINDING user constraint. Don't fold `--underglow` into `brandVars`.
- **Hero slogan "backlit marquee" glow (2026-07-11, 3838bf0)** — `.subtitle` (`HeroSection.module.scss`) = static layered `text-shadow` + two `pointer-events:none` pseudos: `::before` repeating-conic-gradient rotated via `transform` only (46s linear) masked by `radial-gradient(circle closest-side, …)` — the mask MUST be a circle on a perfectly SQUARE box or it renders elliptical and TUMBLES with the rotation into the heading/quick-links; `::after` opacity-only bloom pulse. Both `z-index:-1`, confined by `.subtitle`'s own stacking context (created by fade-in-up's filled transform) — below the glyphs, above the backdrop. `prefers-reduced-motion` freezes both. Never `overflow:hidden` on `.hero` (SearchBar popover); no `filter`/`drop-shadow`/`will-change` per the standing perf gotchas.
- **Phone formatter country-code paste** — `+1 (800) 555-0142` → `(180) 055-5014` if you strip non-digits + slice(0,10). Strip leading `1` when input is 11 digits starting with `1`.
- **Avoid `grid-template-columns: 1fr auto 1fr` with asymmetric side-track content** — `1fr` = `minmax(auto, 1fr)`. Use `position: absolute` on relative parent.
- **Tree-row `1fr auto auto` jams slug behind name on long titles** — promote children to `repeat(auto-fill, minmax(220px, 1fr))`.
- **Supplier-detail panel stretch (Grid+Flex two-tier)** — `.detailGrid { align-items: stretch }`; inner `.panel { display:flex; flex-direction:column }` + `.panelBody { flex:1 }` backfills Description. Sidebar opts out via `align-self: start`.
- **Prefill bus at `@admin/services/prefillBus.ts`** — typed singleton, one-shot `consumePrefill(kind)`. Survives SPA nav, dies on full reload. Forms read via `useState(() => consumePrefill('part'))`.
- **Quick Actions hero strip** at `pages/suppliers/detail/QuickActionsPanel.tsx` — 4-card grid; variants `qaCardPrimary/Blue/Gold/Purple`.
- **Mobile data tables (PartsTable)**: `border-collapse: separate; border-spacing: 0` + 12px radii. `.tableWrap { overflow-x: auto }` ALWAYS. `min-width: 540px` ≤1024px. Description hides ≤1450px; Category ≤768px. Subcategory chips wrap desktop, 2-col on mobile.
- **ColumnHeader sort/filter popover PORTALED to `document.body`** (`createPortal`) — `position: fixed`, viewport clamp, flip-above-if-no-room, close-on-scroll/resize, outside-click + Esc. Two scroll-close guards: `e.target instanceof Node` BEFORE `.contains()`; skip scrolls from inside `.filterList`.
- **Mobile drawer state-machine** (Navbar + AdminLayout): `useState(menuOpen)` + 3 effects on `[menuOpen]` — body-scroll-lock, Esc keydown (attach while open), `[location.pathname]` auto-close. Compositor-only animations.
- **Admin `<aside>` needs conditional `aria-hidden`**: `aria-hidden={!menuOpen ? undefined : false}`. Public drawer `aria-hidden={!menuOpen}` would set `"true"` at desktop where admin sidebar IS visible.
- **`backdrop-filter: blur(2px)` on full-viewport scrim is OK** when scrim only animates opacity. Don't add to elements that translate/scale.
- **Breakpoints in `_variables.scss`**: mobile 768 / tablet 1024 / desktop 1199 / admin-mobile 820 / admin-compact 420. Use `@include responsive(...)`.
- **`--a-blue`/`--a-purple` admin-scope tokens** — defined in `AdminLayout.module.scss .admin` for Dashboard sparklines. Pass as `color="var(--a-blue)"`, NEVER inline hex.
- **Admin Supplier badge = real active sponsorship (2026-06-14)** — `AdminSupplier` has no tier col, so the list/detail badge cross-references sponsor rows: `suppliers/sponsorship.ts` (`buildSponsorshipBySupplier`→supplier's highest **active** tier; `supplierSponsorship`→Platinum/Gold/Silver or **None**) + a None filter chip (`SPONSORSHIP_FILTERS`). Both list+detail fetch `getSponsors()` (best-effort, cancel-flagged). The old `parts_count` SIZE tier (`suppliers/tier.ts` `deriveSupplierTier`, ≥100/≥25, no Featured) survives ONLY as a new-sponsorship hint in `QuickActionsPanel`. `.tierNone` = dashed muted badge.
- **Admin sponsors are API-backed** (was localStorage). `@admin/services/sponsorStore.ts` ASYNC over `adminApi`. Form `<select>` pull REAL UUIDs from `getSuppliers()/getCategories()`.
- **Admin sponsors LIST — column-header sort/filter + supplier→placement link** — reuses the admin PARTS-list LOCAL `ColumnHeader` (admin↛public: PORT it, don't import); tier sorts by `SPONSOR_TIER_RANK`; panel overflow stays visible for the portaled dropdown; `placementPath.ts` builds live links off a cancel-flagged category index. → docs/claude-gotchas/sponsor-boards.md
- **`AdminSponsor.amount` is a runtime NUMERIC string despite its `number` type** (Postgres NUMERIC → JSON string, e.g. `"1500.00"`) — coerce (`amountOf()`/`Number`) before any numeric compare/bucket/sort or it string-compares. Same trap for any `Numeric` column surfaced to TS as `number`.
- **`./deploy.sh --frontend`** = frontend-only path (rebuild frontend + nginx restart, NO api rebuild/seed) — use for pure frontend/SCSS/TS changes; faster and avoids the ~1-2 min `/api` 502 recreate window.
- **Surface backend 4xx `detail` in admin forms via `apiErrorDetail` (`@admin/services/apiError.ts`)** — `axios.isAxiosError(err)` + STRING-detail-only (a 422 `detail` is an ARRAY of error objects that crashes as a React child; a network error has no `response`). Both → `undefined` so the caller falls back to its generic message. Consumers: `sponsors/form` (the single-slot 409 "…already has an active X sponsor. Expire/remove it…") + `reset-password`. Don't re-hand-roll `err.response.data.detail` inline.
- **Theme/route bug repro via SPA NavLink, NOT direct URL** — direct URL remounts everything. Verify SVG persistence across SPA nav: stamp `svg.dataset.sessionMarker` before click, re-check after.
- **Wizard at `@admin/wizard/`** — `data-tour=`/`data-field=` anchors (guard `test_wizard_data_anchors.py`); advance is COMMIT-not-keystroke (blur/Enter/settle, never mid-typing); demo cleanup is DEMO-marker-guarded (refuses to delete unmarked entities); `goBack` guards against the goto LANDING route. → docs/claude-gotchas/frontend.md
- **chrome-devtools-mcp form-fill** — React controlled inputs need native setter: `Object.getOwnPropertyDescriptor(HTMLInputElement.prototype, 'value').set.call(el, v)` + `dispatchEvent(new Event('input', {bubbles:true}))`. Silent-submit triage: `addEventListener('submit', ..., true)` BEFORE click — no submit = HTML5 constraint validation.
- **chrome-devtools-mcp `click(uid)` may miss React Router `<Link>`** — for SPA-nav, use `evaluate_script` + `querySelector(...).click()`. Looping (theme × route): one page + `navigate_page`, NOT `new_page` per cell.
- **Full `docker compose up --build` rebuilds n8n** (313MB re-extract) — `deploy.sh` scopes `build api frontend` only. t3.micro OOMs — stay on t3.small.
- **Frontend Dockerfile = 4 stages** (base/dev/build/prod); compose defaults `prod` (nginx + hashed Vite, no HMR). SCSS/TSX edits need `up --build -d frontend`. Local HMR = `target: dev`.
- **API container has NO volume mount** — edits don't reach via `restart`; use `up -d --build api`. Symptom: seed succeeds but data reflects OLD code.
- **`docker compose restart` traps** — (1) does NOT re-eval `${VAR:-}` from `.env` → `up -d --force-recreate`; (2) WSL2/Docker-Desktop: `restart` AFTER editing a bind-mounted file fails (`mount … no such file`, stale inode) → `up -d --force-recreate <svc>`.
- **`docker compose exec -T <svc> <cmd>` consumes stdin** of wrapping `ssh ... <<HEREDOC` — always `exec -T < /dev/null`.
- **Docker cache can serve stale frontend** — if invisible: `build --no-cache frontend && up -d frontend`.
- **`./deploy.sh --reseed` destructive scope** — TRUNCATE CASCADE is table-level + transitive (ON DELETE irrelevant); `deploy_reseed` hand-carries FIVE restores and ORDER is the design: `users` restored BETWEEN truncate and seed (seed adopts them; customers are never recreated), calendar/messages/bom_shares after, `supplier_feeds` travels as name-keyed TSV, `users.supplier_id` relinked BY NAME. Safety dump `/tmp/pre-reseed-safety.dump`. Deliberately destroyed: `activity_events` + feed-imported parts (~171k on prod — think hard). Census guard pins the ordering. → docs/claude-gotchas/deploy-seed.md
- **`--reseed` can DEADLOCK on a heavy-DDL migration** — `deploy_reseed` recreates api (alembic+seed) THEN `TRUNCATE`s; migration DDL (CREATE TRIGGER/DROP COLUMN = AccessExclusiveLock) races the TRUNCATE → `deadlock detected`, old data persists. Recovery: re-run clear+reseed alone (`docker exec db psql -c 'TRUNCATE …CASCADE'` then `docker exec api python -m app.db.seed`) — PLUS the carry steps `deploy_reseed` wraps around them (backups are still in /tmp on the box; the users restore goes BEFORE the seed, the rest after — see deploy.sh).
- **Prod one-shot SQL fix** (apostrophe/HEREDOC escape hell): `Write tmp/fix.sql → ec2-instance-connect send-ssh-public-key → scp → sudo docker cp → docker exec … psql -f`. Instance Connect key = 60s TTL, push right before each ssh/scp.
- **Dry-run a DESTRUCTIVE prod migration on LIVE data, risk-free** — wrap the migration's exact cleanup + DDL in a scp'd `.sql` as `BEGIN; <select before>; <DELETEs + CREATE INDEX>; <select after>; ROLLBACK;`, run `psql -f`. Proves the real effect on prod data (e.g. migration 016: "11 active Platinum + 7 Gold UNCHANGED, 14 Expired purged, a 2nd active insert raises unique_violation") then UNDOES it — prod untouched. Transactional DDL works (`CREATE INDEX` inside a txn is fine; NOT `CREATE INDEX CONCURRENTLY`). A `--data-only` partial `pg_dump` of one table can't cleanly restore into a fresh DB (missing enum types / FK order), so verify-in-place beats load-a-copy.
- **The fictional demo catalog is flag-gated — `SEED_DEMO_CATALOG`** — code default true, prod compose false; the flag stops resurrection, never deletes (set flag → rebuild → THEN delete rows). Every dependent seed reference stays guarded (an unguarded `suppliers["Kennedy Electronics"]` = prod 502-loop). Guards: `test_seed_demo_flag.py`, `test_compose_env_passthrough.py`. → docs/claude-gotchas/deploy-seed.md
- **Leads CRM (migration 036)** — admin-only sales tooling (manufacturers + aliases/merge-queue + leads + append-only `lead_contacts`). FK DIRECTION IS THE RESEED SAFETY (only bridge: `suppliers.manufacturer_id` SET NULL — guard `test_leads_schema.py`). `manufacturer_canon.py` is the SINGLE canon home. Leads NEVER public; demo refused on READS (`test_leads_never_public.py`). Outcome trio colors are a CVD constraint; `OUTCOME_META` single home `pages/leads/outcome.ts`. → docs/claude-gotchas/deploy-seed.md
- **`test_no_type_url_form_input.py` greps EVERY line incl. COMMENTS** — a comment explaining the type-url gotcha trips the guard; describe the token, never spell it (same class as the XML `--` gotcha).
- **Seed catalog probe is ONE bulk key-set, never per-row (11.9s→1.0s/start)** — the bulk SELECT stays INSIDE `_seed_real_catalog` AFTER `_seed_parts`; every created sku is ADDED to the set (77 dup JSON rows); probe + created-set are `.upper()`-keyed (case-sensitivity was the duplicate generator). → docs/claude-gotchas/deploy-seed.md
- **Fake-presence lever = `circuits --fakeuser` (migrations 034+035)** — `presence_fakes` singleton row: `count` 0-10 (CHECK enforced on BOTH engines) + `names` CSV. `presence_ping` shows the UNION of the count-prefix and named individuals from `FAKE_PRESENCE_ROSTER` (owner-edited fictional names; ids pinned to roster INDEX so identity/palette never reshuffles), for EVERY admin viewer incl. demo. Written ONLY by `scripts/fakeuser.sh` via psql (local or `--prod` over Instance Connect) — no endpoint, no auth surface. `--name <n>` is validated against the roster PARSED FROM THE SOURCE (typos die with the valid list); bare `--down` clears ALL (count AND names). Roster edits are CODE — they need `up -d --build api`, not a reseed. Guard: `test_presence_fakes.py` (16).
- **Category slug changes need `--reseed`** — `get_or_create_category` keys on slug; rename creates a duplicate. Plain `./deploy.sh` won't fix.
- **`./deploy.sh` restarts nginx automatically** (2026-06-02; run `deploy-preflight` first). **Local `nginx.conf` self-heals** (`resolver 127.0.0.11` + variable `proxy_pass`) so a local `up --build <svc>` re-resolves new container IPs; prod `nginx.ssl.conf` relies on the deploy restart.
- **`.dockerignore` in `frontend/` + `api/`** (2026-06-02) — frontend context was 236MB (230MB `node_modules`); now ~5MB. Both Dockerfiles also gain `# syntax=docker/dockerfile:1.6` + BuildKit `--mount=type=cache` on `/root/.npm` (frontend) and `/root/.cache/{uv,pip}` (api). `deploy.sh` exports `DOCKER_BUILDKIT=1` for older Docker. Rebuild on unchanged deps is now near-instant.
- **Deploying API → ~1-2 min of 502 on `/api/*`** — recreating api re-runs `alembic → seed → uvicorn`. HTML still 200. Check api logs (`Seeding database...`) before intervening.
- **nginx HTTP/2 requires `http2 on;`** — `listen 443 ssl;` alone gives HTTP/1.1. All 3 SSL blocks in `nginx.ssl.conf` carry it. Verify: `curl -sI --http2 https://circuitcenter.ai/ -w '%{http_version}\n'` (want `2`).
- **Category preload couples `frontend/index.html` ↔ `api.ts`** — inline `<script>` stashes the fetch promise on `window.__categoryPreload`; `api.getCategory` reuses it (matching slug+params). Direct-load only; SPA nav = axios.
- **`index.html` served `no-cache`** (`frontend/nginx.conf` `location /`) so browsers revalidate the SPA entry. Hashed assets stay `immutable`. Guard: `api/tests/test_nginx_cache_headers.py`.
- **Stale lazy-chunk after deploy → `vite:preloadError` recovery reload** — `@shared/preloadErrorRecovery.ts` (eager entry chunk, called in `main.tsx` BEFORE render) does ONE sessionStorage-guarded reload, fail-open + try/catch-wrapped. Don't remove the `installPreloadErrorRecovery()` call. Guard: `preloadErrorRecovery.test.ts`. → docs/claude-gotchas/frontend.md
- **`sw.js` MUST NOT be cached immutable** — generic regex applies `immutable, 1y` to ALL js. Exact-match `location = /sw.js` + `/registerSW.js` with `no-cache` (exact wins over regex).
- **Sponsor cache invalidation (inc1)** — category endpoints send `no-cache` (set AFTER the 404 check — HTTPException makes a new response; guard `test_cache_headers.py`); `bustSponsorCaches()` (`@admin/services/swCache.ts`) deletes the `api-categories`+`api-general` SW caches, wired in `adminApi` so every sponsor/supplier mutation + wizard-feature busts. **ETag/304 on `/{slug}` + `/partners` via `_conditional_json`** (`If-None-Match`→304); cross-tab push deferred.
- **Edge gzip = `nginx/nginx.ssl.conf`** (`gzip on`/`gzip_proxied any`, json+js) — was OFF (drift). **PROD-only: LOCAL non-SSL nginx does NOT gzip /api — verify on prod** (`per_page=500` 127→24.9 KB, 5.1×). Guard `test_nginx_gzip.py`. **alembic 012** = hot-col indexes (`parts.category_id`/`sub_slug`, `part_listings.part_id`, `price_breaks.listing_id`/`min_quantity`), index-only; guard `test_part_indexes.py`.
- **Workbox `runtimeCaching` regex MUST allow trailing slash** — axios ends `/?params`. Need `\/?` before query: `/\/api\/categories(\/[^/?]+)?\/?(\?.*)?$/`.
- **`global.scss` uses deprecated Sass `darken()`/`lighten()`** — new code uses `@use 'sass:color'` + `color.adjust()`.
- **ProxyHeadersMiddleware trusts all hosts** — required for admin HTTPS URL gen behind nginx. FastAPI 307-redirects missing trailing slash (axios follows).
- **Adding a hostname**: add `server_name` in `nginx.ssl.conf` → stop nginx → `sudo certbot certonly --standalone --expand --cert-name circuitcenter.ai -d <every>` → start. DNS must resolve first. Cert dir is `circuitcenter.ai` (the primary).
- **SMTP creds in `/opt/circuits-com/.env` on prod EC2** (`SMTP_HOST`/587/`no-reply@circuitcenter.ai`; outbound relays through **AWS SES** — domain verified, out of the sandbox). Without `SMTP_HOST`, `email._smtp_send` runs demo. `NOTIFY_RECIPIENTS` = JSON or CSV. **`docker-compose.prod.yml` still defaults `SMTP_HOST` to a dead `mail.hover.com`** (circuits.com-era leftover) — the host `.env` overrides it; drop the default when P2 mail lands.
- **Three auth invariants no change may break (P1, 2026-07-31)** — (1) **demo access keeps working**: `POST /api/auth/demo` is its only door and demo is EXEMPT from the forced reset; (2) **nothing reveals whether an account exists** — unknown-email login still burns `verify_dummy_password()`, records the same failure, returns the same 401; recovery still returns the generic OK; the demo refusal reuses the identical unknown path; (3) the policy lives in **two mirrored homes** (`app/services/password_policy.py` + `@admin/services/passwordPolicy.ts`) — edit one and the live checklist lies to the user. Guards: `tests/test_password_policy.py`, `test_auth_email_login.py`, `test_auth_hardening.py`, `test_auth_forced_password_change.py`, `test_login_rate_limit.py`, `test_auth_recovery.py`, `frontend/src/admin/services/passwordPolicy.test.ts`.
- **`uq_users_email_lower` is a FUNCTIONAL unique index SQLite also honors** — declared on `User.__table_args__` (not migration-only), so `Base.metadata.create_all` reproduces it and any fixture adding two users with the same address in ANY casing now fails at flush. Test fixtures must supply a UNIQUE email per user; `email` is NOT NULL.
- **Prod `ADMIN_SECRET_KEY` MUST be in `/opt/circuits-com/.env`** — `docker-compose.prod.yml` reads it `${...:?}` fail-fast (base default is dev-only), so every prod `up`/deploy errors without it. Rotate via host secret → `up -d --force-recreate api`.
- **n8n no longer in form path** — `routes/forms.py` uses aiosmtplib + the `SMTP_*` relay via BackgroundTasks.
- **Admin login = v13 two-panel (`pages/login/`) + recovery** — **EMAIL-keyed since 2026-07-31** (see "Auth model" above; the field is `email`, `demo`/`demo` no longer signs in — the "See Demo →" button is that account's only door). `forgot-username` is retired server-side (**410**) since the login key IS the address. `LoginPage.module.scss` ports the design under ONE hashed `.authRoot{:global{…}}` (literal classnames, sibling-global keyframes, no `*`/`body` leak). Reset-link origin MUST be `settings.APP_BASE_URL`, NEVER `request.base_url` (poisoning). **Mobile board = vector `<svg>` (`IsoBoardSvg`/`isoGeometry`), desktop = live CSS-3D — see the "fixed-angle 3D = ONE vector svg" gotcha.**
- **Test fixtures must NOT pair a real-looking SMTP host with `SMTP_PASSWORD`** — GitGuardian pattern-matches host+user+pw proximity. Use RFC 6761 TLDs (`.invalid`/`.test`/`.example`). Guard: `api/tests/test_no_smtp_credential_lookalikes.py`. But `EmailStr` rejects those TLDs — smoke tests use plus-tag self-route (`smoke-test+verify@circuitcenter.ai`).
- **Claude Design handoffs** — `WebFetch` saves to `<tmpdir>/tool-results/webfetch-*.bin`; extract `tar -xzf <path>.bin -C design-handoff-vN/`. Versioned dirs gitignored.
- **Accent-colored text on light cards uses `var(--executive-blue)`** — `var(--theme-accent)`/`--theme-cta-bg` fail WCAG-AA. Borders/chip-bg can keep `--theme-accent`. `--fg2` passes on white (4.85:1), fails on `--surface` (4.07:1). Exception: `/keyword` uses `--theme-accent` for section-num + tier price (a11y 91/100 documented).
- **The company logo is `@shared/components/Logo` — per-size OPTICAL BANDS, not one master scaled** — stroke weights change per band, 16px drops the ground stem; do NOT collapse the bands into one geometry+transform. Variants `badge`/`mark`; the badge's hairline rim is load-bearing (the plate reads by EDGE on dark bars). Favicon ladder = per-size PNGs, NO `favicon.svg` on purpose. → docs/claude-gotchas/frontend.md
- **The public navbar is OPAQUE — a translucent near-black bar reads GREY (reverted 2026-08-03)** — the liquid-glass pass made it `background-color: transparent` under a ~.88 veil above 768px. Because glass is disabled ≤768px for GPU cost, **the same page rendered a black bar on mobile and a grey one on desktop**: measured `rgb(39,42,45)` desktop vs `rgb(20,23,25)` mobile against a `--theme-nav-bg` of `rgb(14,17,19)`. Not tunable — 12% of a near-white page through a near-black veil cannot stay near-black; ~.97 matches the ink but leaves no visible effect. `--glass-nav-veil` survives in `_themes.scss` annotated UNCONSUMED (its composited ratios are the measured record). Glass belongs on surfaces that float OVER content (popovers, menus, modals, admin chrome), where the lift is the point rather than a defect.
- **An XML comment may NOT contain `--`** — naming a CSS custom property (`--cc-bench-deep`) inside an `<!-- -->` block in a `.svg` makes the file invalid XML. **Browsers render it anyway; strict parsers reject it outright**, so it ships unnoticed and breaks tooling later. Caught by ImageMagick refusing `mail/roundcube-skin/circuitcenter/images/logo.svg`. Describe the token instead of writing it literally.
- **LOCAL admin passwords are NOT production passwords** — `seed.py` sets its own (`SEED_PW_MATTHEW`, default `admin`) and flags the 4 staff `must_change_password`; prod rows carry whatever each person actually set. Symptom is a **401, not a 500** — the endpoint is healthy and rejecting you, so don't go hunting for a missing column. Repeated attempts arm the in-process rate limiter (5 → 60s, doubling), which `docker compose up -d --force-recreate api` clears. To make local match prod, copy the **bcrypt hash** (plus `must_change_password` + `password_changed_at`) row-to-row — never handle the plaintext. Beware `$` in the hash: quote the heredoc (`<<'SQL'`) or the shell eats `$2b$12$`.
- **`convert` (ImageMagick) has only the internal MSVG renderer here** — no rsvg/inkscape/sharp delegate. It DOES render the logo's arcs correctly, but antialiases poorly at target size (supersample: render 4× then downsample). For anything brand-critical, rasterize through **Chrome** instead (load the SVG into a canvas via chrome-devtools `evaluate_script`, `toDataURL`, base64-decode) — that is how the shipped favicons were made, and they came out ~40% smaller with cleaner edges.
- **The site is PRERENDERED — every route serves its own HTML** — `scripts/seoPrerender.ts` writes one static doc per manifest route at build (regex over the shell, NOT SSR); `frontend/seo-manifest.json` is a COMMITTED snapshot — regen via `node scripts/gen-seo-manifest.mjs` after reseeds/imports or new rows serve the generic shell. Home writes `home.html` NOT `index.html`; nginx `try_files $uri/index.html` is what makes it reachable (guard `test_nginx_seo_prerender.py`). OPEN: prod 132k+ parts needs a cap/tier rule before the next manifest regen. → docs/claude-gotchas/frontend.md
- **`/part/:id` takes a UUID *or* a slug — `getPartDetail` branches on the UUID grammar (2026-08-03)** — it previously only called `/parts/{id}`, so every slug URL was a **soft 404**: nginx served the shell with a 200, `PartPage` set `error`, and since every SEO tag sits behind `{part && …}` the page emitted no title, canonical or Product markup. That was catastrophic rather than cosmetic, because part pages canonicalize to the SLUG form (`partSeo({ slug: part.slug ?? id })`, and slug is always populated) — so ~3,600 working UUID pages were telling Google their canonical was a dead URL. `GET /api/parts/by-slug/{slug}` existed and worked the whole time; nothing called it. Both shapes now render; the UUID form canonicalizes to the slug, which resolves.
- **Container logs had NO rotation — they were filling the prod disk (2026-08-03)** — Docker's default `json-file` driver never rotates: nginx's access log alone reached **238 MB** on a 20 GB root volume at 81%. `docker-compose.prod.yml` now caps all five services at 20MB×3 via a YAML anchor (`x-logging`), and `db`/`n8n` appear in the prod override ONLY to inherit it. **Log opts apply on RECREATE, not restart** — `up -d` (compose detects the change), not `restart`. One-off reclaim that day: `docker builder prune -af` freed **4.05 GB** (566 entries, 0 active) and `journalctl --vacuum-size=200M` freed 1.0 GB → 81%→38%. When disk is tight, check build cache FIRST; `docker system df` under-reports it because the layers live in `overlay2`. n8n's image is 1.57 GB and it is no longer in the form path — the largest remaining reclaim if it is truly unused.
- **The compose `environment:` block is an ALLOWLIST, and an empty `${VAR:-}` DESTROYS the code default** — `Settings` has no `env_file` and the api container has no volume mount, so a value absent from that block never reaches the app however carefully it is written into a host `.env`. Fourth occurrence now: `DEMO_LOGIN_ENABLED` (inert switch), `CALENDAR_*` (calendar unreachable), `STRIPE_*`. Worse than absence — `${CALENDAR_RECIPIENTS:-}` passes the EMPTY STRING, the CSV validator parses it to `[]`, and Settings' four-person default is OVERWRITTEN, not inherited: the job found due events and had nobody to send to. A compose default must MIRROR the code default (as `MAIL_SYNC_MAILBOXES` does). Guards: `test_compose_env_passthrough.py`.
- **Webmail palette = D1 "Instrument Dark"** — the fatigue driver is HALATION and CHROMA, not contrast; contrast is a GATE (4.5 floor), not a ranking; measure a token on the WORST surface it's used on. → docs/claude-gotchas/mail-calendar.md
- **Light→dark CSS migration: every miss is in HOW a value is SPELLED** — rgba-spelled porcelain, escaped hex inside SVG data URIs, white-as-surface vs specular, ink-tinted shadows, same-specificity collisions; re-measure every recorded ratio afterwards. → docs/claude-gotchas/mail-calendar.md
- **Shared calendar (migration 025)** — no owner column; `calendar_reminder_sends` `UNIQUE(event_id, kind, channel)` IS the idempotency arbiter (claim-then-send, at-most-once); filter undeliverable channels BEFORE the claim or the slot burns; `_smtp_send` swallows SMTP errors and returns a bool the job reads. → docs/claude-gotchas/mail-calendar.md
- **`/api/calendar/*` refuses the demo account on READS too** — two doors: plugin shared secret or admin bearer; compare the secret as BYTES with the header side re-encoded latin-1, or a non-ASCII secret 500s/never matches. → docs/claude-gotchas/mail-calendar.md
- **The Roundcube skin ships `ui.js`** — adds compose-close-that-saves-draft (wraps `rcmail.set_draft_id`) and a one-click checkbox-mode toggle; no upstream template forked. → docs/claude-gotchas/mail-calendar.md
- **Signatures need HTML compose** — `htmleditor=1` (NOT `4`), `sig_above=true`; `show_images=1`, **never `2`** (no proxy — "always" lets tracking pixels confirm the address). → docs/claude-gotchas/mail-calendar.md
- **Roundcube 1.6 extension facts that fail SILENTLY** — there is no `identity_save` hook (`identity_form`/`identity_update`/`identity_create`/`identity_delete` are real); the identity form is urlencoded and `type=file` is rewritten to text; `wash_html` runs BEFORE `identity_update`; a prefs `null` is a DELETE; PHP runs as www-data. → docs/claude-gotchas/mail-calendar.md
- **The image ceiling is the CONTAINER (`mem_limit: 192m`), not `memory_limit`** — `Imagick::setResourceLimit()` before every decode, `getimagesize()` probe first, JPEG `setSize()` DCT-scaled read; auto-orient BEFORE `stripImage`, flatten BEFORE JPEG, re-encode LAST (the security boundary). → docs/claude-gotchas/mail-calendar.md
- **ImageMagick here cannot parse valid SVG — glyphs rasterise through headless Chrome** (one sheet screenshot, arithmetic slices, sheet size asserted); brand paths extracted once from simple-icons; LinkedIn/Slack/Skype/CodePen/AngelList absent on purpose. → docs/claude-gotchas/mail-calendar.md
- **`sig_social` builds a CROSS-HOST url and can never stat the file** — `icon_slugs` (generated) gates every `<img>`; an ABSENT list = no icons, never all icons; filenames key off `slugify(title)`, not simple-icons slugs (devto vs devdotto). → docs/claude-gotchas/mail-calendar.md
- **`mail/` has a dependency-free PHP test harness** — `php mail/tests/run.php`; NO composer/PHPUnit ever; local PHP lacks mbstring/gd/imagick (clamp w/ `preg_match(/us)`, never `mb_*`; image tests SKIP LOUDLY — run `mail/tests/run-in-container.sh`). `/opt/circuits-mail/` files are COPIES — scp them; Instance Connect keys expire in 60s. → docs/claude-gotchas/mail-calendar.md
- **`mail/roundcube-plugins/*/config.inc.php` is gitignored only since 2026-08-07** — the `.dist` claimed it for months while `git check-ignore` exited 1; verify ignore rules by RUNNING the command, never by reading docs. → docs/claude-gotchas/mail-calendar.md
- **Stripe (Billing/Invoicing/Tax)** — sales-led quote→subscription, hosted invoice page, webhooks into Postgres; `sponsors` stays the placement authority and a webhook writes `sponsors.status` ONLY, never `tier`/`category_id`. Each tier bills as TWO separately-stated line items with distinct tax codes (advertising vs SaaS): NY's primary-purpose test can tax a bundled charge in full as prewritten software. NO customer self-service — Platinum/Gold are single-slot, DB-enforced by partial unique indexes, so a self-upgrade would race or fail at the constraint. OPEN QUESTION: the grace period after which a failed payment releases an exclusive slot.
- **Stripe live objects (account `acct_1U0vY1DqTxm052QN`; REPRICED 2026-08-22)** — 6 products/6 monthly prices, lookup keys `<tier>_{advertising,platform}_monthly`, 90/10 ad/platform split (Silver $250 / Gold $2,500 / Platinum $10,000 all-in), `tax_behavior: INCLUSIVE`. `unit_amount` and `tax_behavior` are IMMUTABLE — repricing = NEW price objects w/ `transfer_lookup_key=true`, then archive. The 90/10 split is load-bearing NY TAX structure; never stack discounts. One sale = ONE subscription carrying BOTH tier prices. → docs/claude-gotchas/billing-stripe.md
- **Sales quotes from /admin (`stripe_quotes.py` + `admin_quotes.py` + `QuotePanel.tsx`)** — fixed `QUOTE_LADDER` of all-in prices; customers are per-supplier via `metadata.supplier_id` (NEVER email-only); query strings ONLY via httpx `params=`; quote list sponsor-scoped by quote metadata; coupon reuse verifies amount AND duration AND currency; create verifies `amount_total == target` or cancels (cancel wrapped). Panel hides w/o `STRIPE_SECRET_KEY`; demo refused on READS. Guard `test_stripe_quotes.py` (fake FILTERS on query params). → docs/claude-gotchas/billing-stripe.md
- **Stripe webhook receiver (`routes/stripe_webhooks.py` + `services/stripe_webhook.py`)** — hand-rolled HMAC over the header's own `t` STRING + raw body; `t` length-bound BEFORE `int()`; tolerance ONE-SIDED old-only. Secret unset → route 404s — after any deploy touching key-gated features PROBE them (unsigned POST → 400, NEVER 404). `invoice.paid`→Active, `subscription.deleted`→Expired, `payment_failed`→log only; three write-gates (stale event / Paused / slot-conflict w/ rollback) and unactionable payloads ALWAYS ack 200. Cancel subs WITH "void open invoices". Guard `test_stripe_webhook.py`. → docs/claude-gotchas/billing-stripe.md
- **Stripe test sandbox (`acct_1U0vYADbWRRGbHVZ`) + local test-mode (2026-08-15)** — the sandbox mirrors live tax behavior: Silver product/price twins (SAME lookup keys, inclusive, tax codes), head office set, NY + NJ test registrations. Run the LOCAL stack in test mode WITHOUT touching `.env` (the hook blocks `.env` edits anyway): `STRIPE_SECRET_KEY=$(grep '^STRIPE_SECRET_KEY_TEST=' .env | cut -d= -f2-) docker compose up -d --force-recreate api` — shell env beats `.env` in compose interpolation; sessions mint `cs_test_`, card `4242 4242 4242 4242` works; reverting = recreate WITHOUT the override. Stripe can't webhook localhost → a test payment shows the receipt but never materializes the sponsor row (full rehearsal needs `stripe listen --forward-to`). Stripe CLI lives at `~/.local/bin/stripe` (`stripe docs search|api …` beats WebFetch for Stripe docs).
- **Automated cost sync (`cost_sources/` + `jobs/sync_costs.py` + `cost-sync` compose service)** — AWS Cost Explorer 1×/ET-day ($0.01/req, `Application` tag) + Stripe fees hourly (`fee` PLUS `type='stripe_fee'` negatives). Upsert matches (source, vendor, period_start), NEVER touches `source='manual'`, real supersedes `estimate`, reconcile deletes vanished same-source rows; editing a synced row PROMOTES it to manual, DELETE is 409. `deploy.sh` must name cost-sync in BOTH build lists. Prod creds = instance profile via IMDS. → docs/claude-gotchas/billing-stripe.md
- **Self-serve Silver checkout (`routes/checkout.py` + `stripe_checkout.py` + `SilverCheckoutModal`)** — public POST mints a Stripe-HOSTED session; price/tier fixed server-side; real SUBCATEGORY ids or keywords only. Identity resolves ONLY by `stripe_subscription_id` (never the buyer-typed name — a stranger could cancel a live sponsor); self-serve ALWAYS mints a FRESH supplier row. Four webhook gates (paid / sub-id present / amount==$100 / idempotency), all ack 200; sponsor row created ONLY by the signed webhook. Guard `test_checkout.py`. → docs/claude-gotchas/billing-stripe.md
- **`/join` — the merged SENDABLE purchase page** — `/pricing` retired (301 + SPA redirect carrying `search`+`hash`). Stages: 01 tier (Silver price SERVER-PROBED, degrades to desk copy) → 02 placement (Silver board picker deep-links `?sponsor=1`; the page is a ROUTER to boards, never a second checkout) → 03 apply. `?sponsor=1` stays gated on OPEN CAPACITY at BOTH ends (409 full board); a failed boards fetch must NEVER read as sold out. Tier cards are divs w/ `data-selected`, NOT ARIA radios. → docs/claude-gotchas/billing-stripe.md
- **Silver checkout "placement ticket" (`SilverCheckoutModal`)** — confirm + receipt states; `email` prefills AND LOCKS Stripe's hosted field; optional website is load-bearing (webhook copies it onto the supplier). Receipt says PAYMENT RECEIVED, never LIVE (webhook may not have landed); sessionStorage stash is write-after-URL / read-once / 24h TTL / cleared on fresh mount; scrim close arms on `mousedown`; Esc/scrim disabled on the receipt. → docs/claude-gotchas/billing-stripe.md
- **Feed runs are SERVER-OWNED** — POST sync/import STARTS a run (daemon worker, per-part commits) and returns an OBSERVER NDJSON stream; closing detaches, never cancels; `GET /suppliers/{id}/feed-run` reattaches; concurrent start = 409. Cross-process guard = `app/services/feed_lock.py` Postgres ADVISORY lock on its OWN NullPool AUTOCOMMIT connection (`crc32` of the uuid, never `hash()`); the nightly stands down via `FeedRunElsewhere` caught BY NAME and never `_stamp_run`s a skip. Second click = PAUSE (winds down at the next safe part, frees the slot). Guards: `test_feed_lock.py`, `test_feed_run_durability.py`. → docs/claude-gotchas/feeds-catalog.md
- **Auto-import (was "Nightly auto-import") is CONTINUOUS when ON (2026-08-20)** — an interactive import sweeps batch after batch until FeedFatalError (provider quota wall), catalog-dry, a zero-call pass, or the 5000-call RUNAWAY guard (not a budget). Decided SERVER-side from `supplier_feeds.auto_import_enabled` — deliberately NO `continuous` query param (a durable URL must not buy an unbounded quota spend); `?calls` is ignored when ON. The nightly job is never continuous (guard `TestNightlyIsNeverContinuous`).
- **An expired admin token strands the SPA "signed in" (found 2026-08-20)** — `adminApi.onUnauthorized` only clears localStorage; `AuthContext.user` is React state no 401 clears, so ProtectedRoute keeps rendering and every request goes out headerless → FastAPI's raw `Not authenticated` prose. Leads pages recover via `classifyLeadsError` → `logout()` (NEVER a route push — LoginPage bounces authenticated visitors back). THE REST OF THE ADMIN still has the stale-session dead-end — fixing it globally in AuthContext/interceptor is an open follow-up.
- **Distributor feed surface** — `app/services/part_feed/`: `registry.match_provider` keys on WEBSITE domain fragment; keys via `provider_credentials` DB row else env. One NDJSON contract (`sync_event()` sole constructor). SYNC and IMPORT are DISJOINT on "does this supplier already list this part?": `synced` must be 0 on EVERY import run; a part held from ANOTHER distributor falls through to gain `listing_added` — never "simplify" to skip every existing part. Nightly honors `auto_import_enabled`, budget 850 @ 06 UTC; quota wall = FeedFatalError. After big imports: regen seo-manifest + `--frontend` deploy. Runbook `docs/part-import-runbook.md`. → docs/claude-gotchas/feeds-catalog.md
- **BOM tool (`/bom`, migrations 037/038)** — client-side parse (`pages/bom/lib/parseBom.ts`); `POST /api/bom/match` takes identity fields ONLY (`extra="forbid"`, D7 structural); resolve budget 100/day, `FeedFatalError` → `resolve_unavailable` for all remaining. The +20% `SPONSOR_BAND` rule has TWO MIRRORED HOMES (api `bom_match.recommend` ↔ frontend `priceBreaks.ts`, shared test case names). `headerAliases.ts` is GENERATED; `xlsx` = official SheetJS tarball, never the npm name. Shares: 22-char slug, 1MB, 180d TTL, `/bom/s/*` noindex. → docs/claude-gotchas/feeds-catalog.md
- **Sales datasets live at `api/app/db/seed_data/`** — `manufacturers.csv` (company details + part counts) and `leads.csv` (the outreach call list), moved from `temp/` 2026-08-20; the seed reads them (`SEED_MANUFACTURERS`/`SEED_LEADS`, default true everywhere — CSVs are the source of truth, a deploy always restores the roster; lead re-runs INSERT-IF-ABSENT and never touch CRM state). Grow the datasets there.
- **deploy.sh bails on ANY untracked file — with a 2-line warning and EXIT 0 (2026-08-21)** — a prod backup dropped into `temp/` silently killed a deploy that looked successful. Keep backups OUTSIDE the repo (`~/circuits-backups/`); read the deploy output, not just the exit code.
- **The catalog transfer pair (`scripts/catalog_export.py`→`catalog_load.py`, behind `circuits pull/push`) is NATURAL-KEYS ONLY** — per-environment surrogate FKs (parts/suppliers.`manufacturer_id`, migration 036) must never travel: both sides strip them and seed step 5 relinks by canon name on the next api start. Adding a column that FKs a per-env table = add it to the skips or the pull dies on FK violation. Guard: `test_catalog_transfer_scripts.py`.
- **PART IDENTITY is `UNIQUE(manufacturer_id, upper(sku))`** — single home `app/services/part_identity.py`; every create path goes through `get_or_create_part`. NEVER strip punctuation from MPNs (6.8V vs 68V diode; 2.5ft vs 25ft — `test_part_identity.py` pins the exact numbers); 49 real MPN pairs span different manufacturers. Feed-created parts resolve manufacturer through canon AT WRITE TIME; `_adopt_unlinked` claims legacy NULL rows. → docs/claude-gotchas/feeds-catalog.md
- **Migration 041 (identity cleanup)** — merges duplicate parts (survivor = oldest, COALESCE missing fields, `CATEGORY_PREFERENCE` keyed `(MPN, sub_slug)` outranks oldest-wins) then adds both constraints; the listing constraint builds via `CREATE UNIQUE INDEX` + `ADD CONSTRAINT … USING INDEX` (reader-safe); `manufacturer_id` stays NULLABLE (seed drains it — migrations can't import `canon()`); NULLs excluded from the merge (PG GROUP BY treats NULLs as EQUAL). The catalog JSON is the DURABLE authority — guard `test_the_catalog_json_agrees_with_the_preference`. → docs/claude-gotchas/feeds-catalog.md
- **The suite can test Postgres — `tests/pg_harness.py`** — runs a migration's REAL `upgrade()` (`MigrationContext.configure(conn)` + `Operations.context(ctx)`) against local PG inside an always-rolled-back txn at production volume; expose migration SQL as module constants for idempotency re-runs (`DROP TABLE IF EXISTS` all temp tables first); skips when the stack is down, refuses non-local hosts. → docs/claude-gotchas/feeds-catalog.md
- **`Session.begin_nested()` FIRES `after_commit` — do not use it as a savepoint here (SQLAlchemy 2.0.52, measured)** — with or without an enclosing transaction, leaving a `with db.begin_nested():` block commits the session. That silently commits a caller's in-flight work and breaks the importer's per-part commit ordering (`test_each_part_commits_before_its_event` catches it). Use `INSERT … ON CONFLICT DO NOTHING` + re-read instead (`part_identity._insert_if_absent`, dialect-picked, bare conflict target so neither engine has to infer an expression index). And after a Core insert, **never `db.add()` the object you built** — it inserts a second time on flush.
- **The seed was the duplicate GENERATOR** — the catalog JSON lists one chip under several categories with inconsistent capitalisation (`nRF52840-QIAA-R7` in `rf-wireless-ics.json`, `NRF52840-QIAA-R7` in `microcontrollers-processors.json`) and `_seed_real_catalog`'s dedupe probe was case-SENSITIVE, so it created both. That is where every production duplicate came from, and since the seed re-runs on EVERY api container start it would have re-created them after any merge. The probe and the created-set are now `.upper()`-keyed; keep them that way.
- **`UNIQUE(part_id, supplier_id)` on part_listings (2026-08-23)** — one row per distributor per part. `routes/parts.py` used to say its 409 was "the ONLY duplicate protection"; now the DB is. Fixtures that deliberately created two listings for one (part, supplier) were rewritten, not weakened.
- **Concurrency reality: prod runs `--workers 1`**; the default 5+10 pool is FINE (measured: importer holds a connection 2.2ms/part ≈ 12-22% duty → ~70-124 concurrent runs, not 15 — don't resize without a contradicting measurement). Conditional on nothing holding a pooled connection for a run's LIFETIME — the feed lock uses its own NullPool AUTOCOMMIT engine for exactly that reason; any future long-held connection must too. Guard `test_feed_lock.py::TestTheLockDoesNotCompeteWithHttpTraffic` (real PG only). → docs/claude-gotchas/feeds-catalog.md
- **Write waste — FIXED 2026-08-23, keep it fixed** — `lifecycle_verified_at` is stamped INSIDE the `!=` guard (was: every touched part rewrote 8 indexes, ~3GB WAL/import; no consumer reads its VALUE). `part_listings.supplier_id` is indexed (042); `_upsert_listing` passes `noload(price_breaks)`. Guards: `test_feed_part_facts.py`, `test_feed_query_budget.py`. STILL OPEN: `price_breaks_pkey` 40MB / zero scans — composite PK is the only real fix. → docs/claude-gotchas/feeds-catalog.md
- **Price ladders are RECONCILED rung-by-rung, never replaced** — `_sync_price_breaks` diffs on `min_quantity`; the `_storable_price` ROUNDING is load-bearing (feeds quote 5 decimals into `Numeric(10,4)` — a diff without it churns every pass), same for `listing.unit_price`; `_PRICE_SCALE` reads the column type; duplicate `(listing_id, min_quantity)` rows are collapsed. Old-bug fingerprint in `pg_stat_user_tables`: huge ins+del, `n_tup_upd = 0`. Guards: `test_feed_query_budget.py` + `test_price_break_writes_pg.py` (ctid identity; `unit_price` stays unindexed for HOT). → docs/claude-gotchas/feeds-catalog.md
- **OPEN: `price_stale` on the BOM tool measures the wrong thing** — `part_listings.last_updated` has `default=` and NO `onupdate=` (means "created", not "price confirmed"). Do NOT fix by stamping every confirming pass (re-creates the removed churn) — freshness is a property of the FEED (`supplier_feeds.last_synced_at`). Product decision, deliberately deferred. → docs/claude-gotchas/feeds-catalog.md
- **A `Session` bound to a connection that already has a transaction ROLLS BACK its own work on `close()`** — the default `join_transaction_mode="conditional_savepoint"` puts the session's work inside a SAVEPOINT, so a Postgres-harness test that does `Session(bind=conn)` → `flush()` → exit-the-`with` proves NOTHING and every assertion passes by accident (caught while writing `test_price_break_writes_pg.py`: a reprice test reported 0 of 4 rows rewritten). `commit()` releases the savepoint into the outer transaction that the module fixture rolls back. Mutation-test any harness like this — if reverting the fix does not turn the tests red, they are measuring the savepoint, not the code.
- **`circuits pull --catalog` transfers full, RECONCILES on load (2026-08-28 rework)** — the dump is still complete (a watermark can't see deletions) but gzipped on the wire (280MB→35MB), the export is Core tuples not ORM rows (the `selectin` cascade made it 2:06; now ~26s), and the loader diffs against preloaded compare-state instead of delete+reinsert: a no-change pull writes ZERO rows in ~47s (the old loader hydrated a 2.9GB object graph and autoflush-scanned it per listing — killed at 35+ min with <1% done; it would swap-thrash prod on a push). Transfer part identity = `(upper(sku), canon(manufacturer_name))` — sku-only folded the 49 cross-manufacturer MPN pairs into one row and cross-wrote them every pull. Twin source rows (same sku+maker, one unlinked — the open backfill-collision class, 48 locally) export as duplicate lines: FIRST line wins, later twins skip, or pulls oscillate forever instead of converging. Guards: `test_catalog_transfer_scripts.py` (4).
- **The ~39,700 non-Mouser listings are UNREFRESHED, not invented (owner correction, 2026-08-24)** — an earlier agent derived them from data-science research; they are real numbers that simply have no live source and are all stamped 2026-06-03. Do NOT call them fabricated. The problem is staleness beside a live price on a comparison page, which is a credibility issue rather than a fraud one, and the owner's standing call is to leave them alone and handle them separately.
- **NEVER rename a seeded supplier in /admin — it FORKS the row** — seed keys on `Supplier.name` (rename → next boot re-creates the original beside it) and provider matching keys on website FRAGMENTS (both rows matched `digikey`). Fix names in `seed.py` only; `logo_url`/`website` admin edits DO survive a reseed when the name is untouched. Guard `test_provider_supplier_identity.py` (mutation-verified). → docs/claude-gotchas/deploy-seed.md
- **`SEED_DEMO_CATALOG=false` stops resurrection, it does NOT delete (2026-08-24)** — the owner deleted Kennedy Electronics / Mike's Electric repeatedly and they kept returning, because the var is absent from the local `.env` and `docker-compose.yml` defaults it **true** locally (prod defaults false, which is why prod never had them). The correct order is: set the flag, rebuild, THEN delete the rows — deleting first just means the next boot re-creates them with new ids. Verified: after the flag + a delete + a full rebuild, `demo_rows = 0`.
- **Branch workflow**: `master` = deploy tip, `updates` = active dev. Commit on `updates` → push → `checkout master && merge --ff-only updates && push` → deploy → `checkout updates`. No squash; ff-only.

## Brand Colors & Type

Defined in `frontend/src/shared/styles/_variables.scss`:

```
$executive-blue:  #0a4a2e   PCB dark green — headers, hero bg
$nav-blue:        #44bd13   bright green — nav, links, accents
$sponsor-gold:    #a88d2e   sponsor blocks, premium CTAs
$surface:         #eef1f5   page backgrounds
$error-red:       #c0392b   form validation, required-field markers
$font-heading/body  SF Pro / Segoe UI / Inter fallback (native stack, no webfont)
$font-mono          ui-monospace / SF Mono (SKUs, prices, designators)
```

`global.scss` has NO Google Fonts `@import`. h1 letter-spacing `-0.022em`, h2 `-0.015em`.

### Icon system

Public + admin both render Phosphor Light data icons via `<Icon name={x.icon} />` at `@shared/components/Icon.tsx` (regex guard `/^[a-z][a-z0-9-]*$/` no-ops on non-Phosphor strings). Font self-hosted at `frontend/public/fonts/phosphor-light/` (~1.2MB), loaded once in `frontend/index.html`. Admin **sidebar nav** uses Phosphor; topbar controls (Search/Bell/Plus/Menu) stay Lucide.

Names live in `api/app/db/seed.py` (15 + 75 = 90 strings). New category: kebab-case Phosphor name from https://phosphoricons.com — no `ph-` prefix. `Category.icon` is `String(40)` (alembic 005); regression asserts column metadata length ≥24 (SQLite ignores VARCHAR len).

**A Phosphor icon is ONE font character — its sub-parts cannot animate independently** — to animate a piece of a glyph (e.g. the Import card's falling-arrows "downloading" stream, 5852649), swap in an inline `<svg>` that redraws the same anatomy in separate nodes, `stroke="currentColor"` so the filled-card color cascade still applies, transform+opacity keyframes only, and a `prefers-reduced-motion` state that shows ONE static element (frozen stacked arrows at resting `opacity: 0` = invisible icon).

**Don't render `{x.icon}` as a raw text-node** — it'll display the literal name. Pre-commit guard:
```bash
grep -rn ">{[a-zA-Z_]*\.icon}<\|>{[a-zA-Z_]*\.category_icon}<" frontend/src --include="*.tsx"
# Empty = clean. Sweep frontend/src (not just /public).
```
`<option>` labels are HTML-text-only — drop any `${cat.icon}` prefix. When debugging icon rendering, screenshot — `getComputedStyle().content` is empty `""` for Phosphor's PUA glyphs in Chromium serializer even though the glyph paints.
